services:
  api:
    build: .
    container_name: developer-landing-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Персистентность файлового хранилища между перезапусками.
      - ./data:/app/data
    restart: unless-stopped
